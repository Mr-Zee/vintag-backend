{
  "version": 2,
  "builds": [
    {
      "src": "src/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "source": "/api/(.*)", "dest": "src/server.js" },
    { "source": "/(.*)", "dest": "src/server.js" }
  ]
}